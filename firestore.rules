rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write access for development purposes.
    // WARNING: These rules are not secure for a production environment.
    match /{document=**} {
      allow read, write: if request.time > timestamp.date(1970, 1, 1);
    }
  }
}
