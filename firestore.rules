
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access for development purposes.
    // In a production app, you would restrict this further.
    // For example, only allow authenticated users to write to their own data.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
